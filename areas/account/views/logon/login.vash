@{
	
}
@html.extend('_layout', function(model) {
    @html.block('content', function(model) {
        <form method="post">
            <input type="text" name="User.UserName" value="admin" />
            <input type="password" name="User.Password" value="admin" />
            <input type="checkbox" name="User.Remember" value="true" />
            <input type="submit" value="Submit" />
        </form>
    })
})