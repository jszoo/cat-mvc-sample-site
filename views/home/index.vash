
@html.extend('_layout', function(ctx) {

    @html.block('content', function(ctx) {

        <h1 class="name">Base on @model.title</h1>
        <p>Thanks for looking at cat mvc sample site!</p>
    })

})